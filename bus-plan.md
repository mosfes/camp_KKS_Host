# แผนพัฒนาระบบสถานะนักเรียนบนรถ

## 1. เป้าหมาย

สร้างระบบสำหรับดูว่านักเรียนคนใดอยู่บนรถในขณะนั้น โดยเน้นให้ขั้นตอนหน้างานสั้นที่สุด:

- นักเรียนเป็นผู้กด `อยู่บนรถแล้ว` ด้วยตนเองทุกครั้งที่ขึ้นรถ
- ครูไม่ต้องเดินเช็คชื่อนักเรียนซ้ำก่อนรถออก
- เมื่อรถถึงจุดแวะ ครูเปลี่ยนสถานะรถเป็น `จอด` และเลือกวิธีจัดการนักเรียนบนรถ
- ครูเลือกได้ว่าจะเคลียร์นักเรียนทั้งหมดทันที หรือให้นักเรียนกดลงจากรถเองทีละคน
- ไม่มีปุ่ม `นักเรียนลงรถทั้งหมด` แยกต่างหาก
- รองรับรถชั้นเดียวและรถสองชั้น
- มีผังตำแหน่งแบบมาตรฐาน 2 ที่นั่ง — ทางเดิน — 2 ที่นั่ง โดยไม่ต้องอิงเลขที่นั่งจริงของรถทัวร์

## 2. สถานะที่ผู้ใช้เห็น

ระบบมีสถานะให้น้อยที่สุดตามที่กำหนด

### สถานะรถ

- `จอด`
- `กำลังเดินทาง`

### สถานะนักเรียน

- `ไม่ได้อยู่บนรถ`
- `อยู่บนรถ`

ระบบอาจเก็บเวลาและประวัติเหตุการณ์ภายในเพื่อการตรวจสอบย้อนหลัง แต่จะไม่เพิ่มสถานะอื่นในหน้าจอผู้ใช้

## 3. กติกาหลักของระบบ

1. รถที่เพิ่งสร้างมีสถานะ `จอด`
2. ในเที่ยวแรก ครูสามารถเลือกนักเรียนจากหน้าครูและยืนยันว่าอยู่บนรถได้ เมื่อนักเรียนมีที่นั่งและรถจอดอยู่
3. หลังรถออกรถครั้งแรกแล้ว นักเรียนกด `อยู่บนรถแล้ว` ได้เมื่อรถของตนมีสถานะ `จอด` เท่านั้น
4. นักเรียนกดได้เฉพาะรถที่ตนถูกจัดไว้ และไม่สามารถเลือกรถหรือที่นั่งเอง
5. เมื่อกดสำเร็จ สถานะของนักเรียนเปลี่ยนเป็น `อยู่บนรถ`
6. เมื่อครูเปลี่ยนรถจาก `จอด` เป็น `กำลังเดินทาง` สถานะนักเรียนไม่เปลี่ยน
7. ระหว่างรถ `กำลังเดินทาง` นักเรียนไม่สามารถกดยืนยันขึ้นรถเพิ่มได้
8. เมื่อครูเปลี่ยนรถจาก `กำลังเดินทาง` เป็น `จอด` ครูเลือกได้ว่าจะล้างสถานะนักเรียนทั้งหมดทันที หรือให้นักเรียนกดลงรถเอง
9. การกด `จอด` ซ้ำในขณะที่รถจอดอยู่แล้วต้องไม่ล้างสถานะนักเรียนที่เพิ่งกดขึ้นรถสำหรับเที่ยวถัดไป
10. หากครูเลือกให้นักเรียนลงรถเอง นักเรียนที่อยู่บนรถมีปุ่มกด `ลงจากรถแล้ว` เมื่อรถจอด
11. การกด `จอด` หมายถึงรถถึงจุดแวะ ไม่ใช้แทนการจอดติดไฟแดงหรือจอดชั่วคราวระหว่างทาง

## 4. ขั้นตอนตั้งค่าครั้งแรกฝั่งครู

ขั้นตอนนี้ทำจากหน้ารายละเอียดค่าย โดยทำหนึ่งครั้งต่อรถของค่าย หรือทำใหม่เมื่อมีการเปลี่ยนรถ/เปลี่ยนการจัดที่นั่ง

### 4.1 สร้างรถ

เมื่ออยู่ในหน้ารายละเอียดค่ายแล้ว หัวหน้าค่ายหรือแอดมินเลือกเฉพาะ:

- ระดับชั้น
- ห้องเรียนที่จะใช้รถ (หนึ่งห้องเรียนต่อหนึ่งคัน)
- ชื่อรถที่ใช้ภายในระบบ เช่น `คันที่1 ก123`
- จำนวนชั้นของรถ: 1 หรือ 2 ชั้น
- จำนวนแถวของแต่ละชั้น

ไม่ต้องเลือกค่าย เพราะค่ายถูกกำหนดจากหน้ารายละเอียดค่ายและเส้นทางของหน้านั้นอยู่แล้ว ระบบใช้ครูประจำชั้นจากห้องเรียนที่ผูกกับรถโดยอัตโนมัติ

### 4.2 สร้างผังตำแหน่งอัตโนมัติ

แต่ละแถวใช้รูปแบบคงที่ 2–2:

```text
ด้านหน้ารถ

[A01] [B01]    [C01] [D01]
[A02] [B02]    [C02] [D02]
[A03] [B03]    [C03] [D03]
```

- `A` และ `D` เป็นที่นั่งติดหน้าต่าง ส่วน `B` และ `C` เป็นที่นั่งติดทางเดิน
- รถชั้นเดียวและรถสองชั้นใช้รหัส `A01`, `B01`, `C01`, `D01`, ... เหมือนกัน
- รถสองชั้นแยกชั้นจากตัวเลือก `ชั้นบน` / `ชั้นล่าง` ในผัง จึงไม่เติมข้อความกำกับชั้นในรหัสที่นั่ง
- ระบบสร้างตำแหน่งจากจำนวนแถวโดยอัตโนมัติ
- ระบบคำนวณคนนั่งข้างกันจากตำแหน่ง ไม่เก็บชื่อเพื่อนข้างที่นั่งซ้ำอีกชุด
- คู่ที่นั่งในหนึ่งแถวคือ A–B และ C–D

### 4.3 ครูเดินตรวจและจัดนักเรียนลงผังครั้งแรก

1. ครูเปิดหน้า `จัดผังรถครั้งแรก`
2. ครูหรือหัวหน้าห้องที่ได้รับโทรศัพท์ครูเดินจากด้านหน้ารถไปด้านหลัง
3. แตะตำแหน่งแรก แล้วเลือกนักเรียนที่นั่งตำแหน่งนั้น
4. ระบบเลื่อนไปตำแหน่งถัดไปอัตโนมัติ เพื่อให้ลงชื่อเรียงต่อกันได้เร็ว
5. รายชื่อที่ถูกเลือกแล้วจะหายออกจากรายการรอจัด
6. สามารถย้อนกลับ แก้ตำแหน่ง หรือสลับสองคนได้ระหว่างขั้นตอนตั้งค่า
7. ระบบแสดงจำนวน `จัดแล้ว / นักเรียนทั้งหมด` และรายชื่อที่ยังไม่ได้จัด
8. เมื่อครบ ครูตรวจภาพรวมและกด `บันทึกผัง`

หัวหน้าห้องไม่ต้องมีสิทธิ์ครูในระบบ เพราะเป็นการช่วยกรอกผ่านโทรศัพท์ที่ครูเปิดไว้ การกดบันทึกผังควรให้ครูเป็นผู้ตรวจและกดขั้นสุดท้าย

### 4.4 หลังบันทึกผัง

- นักเรียนไม่สามารถเปลี่ยนตำแหน่งเอง
- ครูประจำชั้นของห้องที่ผูกกับรถ หัวหน้าค่าย หรือแอดมินเท่านั้นที่แก้ผังได้ตามสิทธิ์
- ควรเก็บผู้แก้ เวลา และตำแหน่งก่อน/หลังไว้ในประวัติ
- ตำแหน่งว่างสามารถปล่อยว่างหรือปิดใช้งานได้

## 5. ขั้นตอนใช้งานในแต่ละช่วงการเดินทาง

### 5.1 ก่อนออกเดินทาง

1. รถอยู่ในสถานะ `จอด`
2. นักเรียนเปิดหน้าค่ายของตน
3. หน้าเว็บแสดงชื่อรถ ชั้น ตำแหน่ง และชื่อคนที่นั่งข้างกัน
4. เที่ยวแรกครูเลือกเฉพาะนักเรียนที่ร่วมเดินทางลงในผัง
5. หลังจากรถเคยออกรถแล้ว นักเรียนขึ้นรถและกด `อยู่บนรถแล้ว` ด้วยตนเอง
6. ปุ่มเปลี่ยนเป็นเครื่องหมายยืนยันและแสดง `อยู่บนรถ`
7. หน้าครูอัปเดตจำนวนและสีบนผังแบบอัตโนมัติ
8. ครูดูรายชื่อผู้ที่ยังเป็น `ไม่ได้อยู่บนรถ`
9. เมื่อพร้อมออก ครูกดเปลี่ยนรถเป็น `กำลังเดินทาง`

หากยังมีนักเรียนไม่ได้จัดที่นั่ง ระบบต้องแสดงรายชื่อและให้ครูยืนยันว่าไม่ร่วมเดินทางในเที่ยวนี้ จากนั้นจึงบันทึกผังและอนุญาตให้ออกรถได้

### 5.2 ระหว่างเดินทาง

- รถแสดงสถานะ `กำลังเดินทาง`
- นักเรียนที่กดแล้วคงสถานะ `อยู่บนรถ`
- นักเรียนที่ยังไม่ได้กดไม่สามารถกดย้อนหลังระหว่างรถกำลังเดินทาง
- ครูดูผังและยอดรวมได้ แต่ไม่ต้องเดินเช็คซ้ำ

### 5.3 เมื่อถึงจุดแวะหรือปลายทาง

1. ครูกดปุ่ม `รถจอดถึงจุดแวะ`
2. ระบบให้เลือก `ให้นักเรียนลงรถเอง` หรือ `ลงรถทุกคน`
3. ถ้าเลือกลงรถทุกคน ระบบทำงานในคำสั่งเดียว:
   - เปลี่ยนรถเป็น `จอด`
   - เปลี่ยนนักเรียนทุกคนของรถที่เป็น `อยู่บนรถ` ให้เป็น `ไม่ได้อยู่บนรถ`
   - บันทึกเวลา สถานที่แวะถ้ามี และครูผู้กด
4. ถ้าเลือกให้นักเรียนลงรถเอง ระบบเปลี่ยนรถเป็น `จอด` และนักเรียนแต่ละคนกด `ลงจากรถแล้ว` เอง
5. เมื่อจะออกจากจุดแวะ นักเรียนที่ลงรถแล้วต้องกด `อยู่บนรถแล้ว` ใหม่

ไม่มีปุ่ม `ลงรถทั้งหมด` แยกต่างหาก แต่ครูเลือก `ลงรถทุกคน` ได้จากหน้าต่างยืนยันการจอด

## 6. รถสองชั้น

หน้าครูมีตัวเลือก:

- `ภาพรวม`
- `ชั้น 1`
- `ชั้น 2`

ตัวอย่างสรุป:

```text
รถ ม.5/1        อยู่บนรถ 37 / 40 คน
ชั้น 1          อยู่บนรถ 18 / 20 คน
ชั้น 2          อยู่บนรถ 19 / 20 คน
```

ผังแต่ละชั้นแสดงแยกกัน แต่สถานะ `จอด` และ `กำลังเดินทาง` เป็นสถานะของรถทั้งคัน การกดรถจอดหนึ่งครั้งจะใช้ตัวเลือกเดียวกันกับทั้งสองชั้น โดยเคลียร์สถานะทั้งหมดหรือรอให้นักเรียนกดลงรถเอง

## 7. หน้าจอฝั่งครู

### 7.1 หน้าภาพรวมระดับชั้น

หัวหน้าค่ายและแอดมินเห็น:

- ตัวเลือกระดับชั้น
- รถทุกคันในระดับชั้นนั้น
- สถานะรถแต่ละคัน
- จำนวน `อยู่บนรถ / นักเรียนที่จัดไว้`
- จำนวนคนที่ยังไม่ได้อยู่บนรถ
- ปุ่มเปิดดูผังของแต่ละคัน
- คำสั่งเปลี่ยนรถเป็น `จอด` หรือ `กำลังเดินทาง`

ถ้าจะมีคำสั่งระดับชั้น เช่น `รถระดับชั้นนี้จอดถึงจุดแวะทั้งหมด` ต้องแสดงรายชื่อรถและจำนวนผู้โดยสารที่จะถูกเปลี่ยนสถานะก่อนยืนยัน

### 7.2 หน้ารถแต่ละคัน

- ชื่อรถ ห้องเรียน และครูประจำชั้นของห้องที่ผูกกับรถ
- สวิตช์สถานะ `จอด` / `กำลังเดินทาง`
- จำนวนรวมของนักเรียน
- ผังรถแบบสี
- ตัวกรอง `ทั้งหมด`, `อยู่บนรถ`, `ไม่ได้อยู่บนรถ`
- สลับมุมมอง `ผังรถ` / `รายชื่อ`
- ช่องค้นหาชื่อนักเรียน
- เวลาอัปเดตล่าสุด

สีบนผัง:

- สีเขียว: `อยู่บนรถ`
- สีเทา: `ไม่ได้อยู่บนรถ`
- ช่องว่าง: ไม่มีผู้ถูกจัดในตำแหน่งนั้น

ไม่เพิ่มสีหรือสถานะอื่นนอกเหนือจากสองสถานะนักเรียน

## 8. หน้าจอฝั่งนักเรียน

หน้าเว็บแสดง:

- ชื่อรถ
- ชั้นของรถ
- หมายเลขตำแหน่ง
- ชื่อนักเรียนที่นั่งข้างกัน
- สถานะรถ
- สถานะของนักเรียน
- ปุ่ม `อยู่บนรถแล้ว` เมื่อรถจอดและนักเรียนยังไม่ได้ยืนยัน

เมื่อรถกำลังเดินทาง ปุ่มขึ้นรถ/ลงรถต้องถูกปิด พร้อมข้อความให้รอรถจอด

นักเรียนไม่เห็นปุ่มเปลี่ยนรถ เปลี่ยนตำแหน่ง หรือเปลี่ยนสถานะกลับเป็นไม่ได้อยู่บนรถ

## 9. สิทธิ์การใช้งาน

### แอดมิน

- สร้าง แก้ไข และลบรถในทุกค่าย
- เลือกดูได้ทุกระดับชั้น ทุกห้อง และทุกคัน
- จัดและแก้ผังรถ
- เปลี่ยนสถานะรถทุกคัน

### หัวหน้าค่าย

- จัดการรถทั้งหมดในค่ายที่ตนรับผิดชอบ
- เลือกดูและสั่งงานตามระดับชั้น
- แก้ผังรถและจัดการรถทั้งหมดในค่าย
- เปลี่ยนสถานะรถทุกคันในค่าย

### ครูประจำชั้น

- ระบบผูกสิทธิ์จากห้องเรียน ไม่ต้องตั้งค่าครูซ้ำในข้อมูลรถ
- เห็นและจัดการได้เฉพาะรถ/นักเรียนของห้องตนเอง
- รถของห้องตนเองจะถูกแสดงให้อัตโนมัติ เพราะหนึ่งห้องเรียนผูกกับหนึ่งคัน
- ไม่มีสิทธิ์เปลี่ยนรถหรือข้อมูลของห้องอื่น

### นักเรียน

- เห็นเฉพาะข้อมูลรถ ตำแหน่ง และสถานะของตนเอง
- กดได้เฉพาะ `อยู่บนรถแล้ว` ตามเงื่อนไขของรถ

ทุก API ต้องตรวจสิทธิ์ฝั่งเซิร์ฟเวอร์ ห้ามพึ่งการซ่อนปุ่มในหน้าเว็บเพียงอย่างเดียว

## 10. โครงสร้างข้อมูลที่เสนอ

ชื่อจริงสามารถปรับตามแนวทางตั้งชื่อของ Prisma ในโครงการ แต่ควรแยกความรับผิดชอบดังนี้

### `camp_bus`

- รหัสรถ
- ค่าย
- ห้องเรียน/ระดับชั้นที่ผูก
- ชื่อรถภายในระบบ
- จำนวนชั้น
- สถานะรถ `PARKED` หรือ `TRAVELING`
- ห้องเรียนที่ผูกกับรถแบบหนึ่งต่อหนึ่ง โดยใช้ครูประจำชั้นจากข้อมูลห้องเรียนเดิม
- ตำแหน่งที่จัดไว้ (ไม่มีการล็อกผังในระบบ)
- เวลาสร้างและแก้ไขล่าสุด
- ข้อมูลเสริม เช่น เลขรถบริษัท คนขับ และเบอร์โทร

### `camp_bus_floor`

- รถ
- หมายเลขชั้น
- จำนวนแถว

### `camp_bus_position`

- ชั้นของรถ
- แถว
- ฝั่งซ้าย/ขวา
- ลำดับด้านใน/ด้านนอก
- ป้ายที่แสดง เช่น `A01` หรือ `D03`
- เปิดใช้งานหรือไม่

### `camp_bus_student`

- รถ
- นักเรียนที่ลงทะเบียนค่าย
- ตำแหน่งบนรถ
- สถานะปัจจุบัน `OFF_BUS` หรือ `ON_BUS`
- เวลาที่กดอยู่บนรถล่าสุด
- หมายเลขรอบการขึ้นรถปัจจุบัน

ข้อบังคับสำคัญ:

- รถหนึ่งคันผูกกับห้องเรียนเดียว
- ห้องเรียนหนึ่งห้องผูกกับรถได้หนึ่งคันในค่ายเดียวกัน
- นักเรียนหนึ่งคนอยู่ได้หนึ่งรถต่อค่าย
- ตำแหน่งหนึ่งมีนักเรียนได้หนึ่งคน
- นักเรียนหนึ่งคนมีหนึ่งตำแหน่งในรถ

### `camp_bus_event`

เก็บประวัติภายในโดยไม่เพิ่มสถานะบนหน้า UI:

- รถ
- นักเรียน ถ้าเป็นเหตุการณ์ของนักเรียน
- ประเภทเหตุการณ์ เช่น ยืนยันขึ้นรถ, เริ่มเดินทาง, รถจอดและล้างสถานะ
- เวลาที่เกิด
- ผู้ดำเนินการ
- รอบการเดินทาง
- ข้อมูลประกอบที่จำเป็น

## 11. API ที่เสนอ

### ฝั่งครู

- `POST /api/camps/[campId]/buses` สร้างรถและผังตำแหน่ง
- `GET /api/camps/[campId]/buses` ดูรถตามสิทธิ์ พร้อมตัวกรองระดับชั้น
- `GET /api/camps/[campId]/buses/[busId]` ดูผังและสถานะนักเรียน
- `PUT /api/camps/[campId]/buses/[busId]` แก้ข้อมูลรถ
- `PUT /api/camps/[campId]/buses/[busId]/layout` จัดหรือแก้ผังครั้งแรก
- `POST /api/camps/[campId]/buses/[busId]/status` เปลี่ยน `จอด` / `กำลังเดินทาง`
- `POST /api/camps/[campId]/bus-status/by-grade` เปลี่ยนสถานะรถหลายคันตามระดับชั้นสำหรับหัวหน้าค่าย/แอดมิน

### ฝั่งนักเรียน

- `GET /api/student/camps/[campId]/bus` ดูรถ ตำแหน่ง เพื่อนข้างที่นั่ง และสถานะตนเอง
- `POST /api/student/camps/[campId]/bus/board` กด `อยู่บนรถแล้ว`
- `POST /api/student/camps/[campId]/bus/alight` กด `ลงจากรถแล้ว`

## 12. ความถูกต้องของข้อมูลที่ต้องระวัง

### เปลี่ยนรถเป็นจอด

ต้องทำภายใน transaction เดียว:

1. ตรวจว่ารถเดิมเป็น `กำลังเดินทาง`
2. เปลี่ยนรถเป็น `จอด`
3. เปลี่ยนนักเรียนของรถจาก `อยู่บนรถ` เป็น `ไม่ได้อยู่บนรถ`
4. บันทึก event และเวลา

หากรถจอดอยู่แล้ว การเรียกซ้ำต้องตอบว่าสำเร็จแบบไม่ทำอะไร และห้ามล้างสถานะซ้ำ เพราะอาจมีนักเรียนเริ่มกดยืนยันสำหรับเที่ยวใหม่แล้ว

### นักเรียนกดขึ้นรถ

- ตรวจ session นักเรียน
- ตรวจว่านักเรียนลงทะเบียนค่ายแล้ว
- ตรวจว่านักเรียนถูกจัดในรถและมีตำแหน่ง
- ตรวจว่ารถเป็น `จอด`
- เปลี่ยนสถานะด้วยคำสั่งแบบ idempotent เพื่อให้กดซ้ำหรือเครือข่าย retry แล้วไม่สร้างข้อมูลซ้ำ
- บันทึกเวลาและ event

### เปลี่ยนรถเป็นกำลังเดินทาง

- ตรวจสิทธิ์ครูต่อรถ/ห้อง/ค่าย
- ตรวจว่ารถเดิมเป็น `จอด`
- แสดงจำนวนนักเรียนที่ยังไม่ได้อยู่บนรถก่อนยืนยัน
- เปลี่ยนเฉพาะสถานะรถ ไม่เปลี่ยนสถานะนักเรียน

## 13. การอัปเดตหน้าจอ

- หน้าครูควรอัปเดตจำนวนและผังอัตโนมัติทุก 5–10 วินาทีในรุ่นแรก
- มีปุ่มรีเฟรชด้วยตนเองเป็นทางสำรอง
- ระยะถัดไปจึงค่อยพิจารณา realtime หากมีผู้ใช้จำนวนมากและต้องการเห็นทันที
- หน้าเว็บต้องออกแบบแบบ mobile-first เพราะครูและนักเรียนใช้งานบนโทรศัพท์เป็นหลัก

## 14. ลำดับการพัฒนา

### ระยะ 1: ฐานข้อมูลและสิทธิ์

- [ ] เพิ่ม enum สถานะรถและนักเรียน
- [ ] เพิ่มตารางรถ ชั้น ตำแหน่ง การจัดนักเรียน และ event
- [ ] เพิ่ม unique constraints ป้องกันรถ/ตำแหน่ง/นักเรียนซ้ำ
- [ ] ทำ helper ตรวจสิทธิ์แอดมิน หัวหน้าค่าย และครูประจำชั้น โดยตรวจจาก role เดิมร่วมกับความสัมพันธ์ของครูต่อค่าย ห้องเรียน และรถ
- [ ] เพิ่ม migration โดยไม่แก้ข้อมูลเดิมของระบบเช็คชื่อ

### ระยะ 2: ตั้งค่าครั้งแรกฝั่งครู

- [ ] หน้าสร้างรถภายในรายละเอียดค่าย โดยเลือกเฉพาะระดับชั้นและห้องเรียน
- [ ] รองรับ 1 หรือ 2 ชั้น และจำนวนแถวแยกแต่ละชั้น
- [ ] สร้างตำแหน่งแบบ 2–2 อัตโนมัติ
- [ ] หน้าจัดนักเรียนเรียงตามตำแหน่ง
- [ ] แสดงคนยังไม่ถูกจัดและตำแหน่งว่าง
- [ ] ยืนยันและบันทึกผัง

### ระยะ 3: นักเรียนกดอยู่บนรถ

- [ ] เพิ่มข้อมูลรถในหน้าค่ายของนักเรียน
- [ ] แสดงรถ ชั้น ตำแหน่ง และคนนั่งข้างกัน
- [ ] เพิ่มปุ่ม `อยู่บนรถแล้ว`
- [ ] จำกัดการกดเฉพาะตอนรถจอด
- [ ] ป้องกันการกดซ้ำและ request ซ้ำ

### ระยะ 4: หน้าควบคุมฝั่งครู

- [ ] ภาพรวมตามระดับชั้นและตามรถ
- [ ] ผังรถชั้นเดียว/สองชั้น
- [ ] สีสถานะอยู่บนรถ/ไม่ได้อยู่บนรถ
- [ ] มุมมองรายชื่อและค้นหา
- [ ] เปลี่ยนรถเป็นกำลังเดินทางพร้อมคำเตือนคนที่ยังไม่กด
- [ ] เปลี่ยนรถเป็นจอดและล้างสถานะนักเรียนแบบ transaction
- [ ] คำสั่งระดับชั้นเฉพาะหัวหน้าค่ายและแอดมิน

### ระยะ 5: ประวัติและความพร้อมใช้งานจริง

- [ ] หน้าประวัติการเดินทางสำหรับผู้มีสิทธิ์
- [ ] บันทึกผู้กด เวลา และการเปลี่ยนผัง
- [ ] รองรับ retry เมื่ออินเทอร์เน็ตไม่เสถียร
- [ ] เพิ่ม audit log โดยไม่เปิดข้อมูลเกินสิทธิ์
- [ ] ทดสอบบนมือถือจริงหลายขนาด

## 15. เกณฑ์ทดสอบรับงาน

- [ ] สร้างรถชั้นเดียว 10 แถวแล้วได้ 40 ตำแหน่งเรียงถูกต้อง
- [ ] สร้างรถสองชั้นโดยจำนวนแถวแต่ละชั้นต่างกันได้
- [ ] จัดนักเรียนลงตำแหน่งและบันทึกผังได้
- [ ] ระบบบอกคนนั่งข้างกันถูกต้องหลังแก้หรือสลับตำแหน่ง
- [ ] ครูประจำชั้นไม่สามารถเปิดหรือแก้รถของห้องอื่นผ่าน API ได้
- [ ] หัวหน้าค่ายและแอดมินกรองตามระดับชั้นและดูทุกคันในขอบเขตตนได้
- [ ] นักเรียนไม่สามารถกดให้เพื่อนหรือกดขึ้นรถคันอื่นได้
- [ ] นักเรียนกด `อยู่บนรถแล้ว` ขณะรถจอดได้
- [ ] นักเรียนกดขณะรถกำลังเดินทางไม่ได้
- [ ] การกดซ้ำไม่สร้าง event หรือข้อมูลสถานะซ้ำผิดพลาด
- [ ] ครูเปลี่ยนรถเป็นกำลังเดินทางแล้วสถานะนักเรียนคงเดิม
- [ ] ครูเปลี่ยนรถจากกำลังเดินทางเป็นจอดแล้วนักเรียนทั้งคันเป็นไม่ได้อยู่บนรถ
- [ ] รถสองชั้นถูกล้างสถานะทั้งสองชั้นพร้อมกัน
- [ ] การส่งคำสั่งจอดซ้ำไม่ล้างนักเรียนที่กดขึ้นรถสำหรับเที่ยวใหม่
- [ ] การเปลี่ยนสถานะรถและนักเรียนสำเร็จหรือย้อนกลับทั้งหมด ไม่มีข้อมูลค้างครึ่งหนึ่ง
- [ ] หน้าครูแสดงยอดรวมตรงกับผังและรายชื่อ

## 16. ขอบเขต MVP

งานรุ่นแรกควรมี:

- รถหนึ่งคันผูกกับหนึ่งห้องเรียน
- รถ 1 หรือ 2 ชั้น
- ผัง 2–2 และจำนวนแถวกำหนดได้
- ครูจัดที่นั่งครั้งแรกและบันทึกผัง
- นักเรียนกดอยู่บนรถเอง
- ครูเปลี่ยนรถระหว่างจอด/กำลังเดินทาง
- เมื่อรถจอด ระบบเปลี่ยนนักเรียนทั้งคันเป็นไม่ได้อยู่บนรถ
- หน้าครูดูผังและรายชื่อ
- สิทธิ์ตามบทบาทและห้องเรียน
- เก็บประวัติภายใน

สิ่งที่เลื่อนไประยะถัดไป:

- ผังรถแบบอื่นนอกจาก 2–2
- GPS หรือการบังคับกดเฉพาะพื้นที่รถ
- QR/PIN ประจำเที่ยว
- การแจ้งเตือนผู้ปกครอง
- realtime แบบ WebSocket

## 17. จุดเชื่อมกับระบบเดิม

- ใช้ข้อมูลค่าย ห้องเรียน ครูประจำชั้น และนักเรียนที่ลงทะเบียนจาก schema เดิม
- แยกระบบรถออกจาก `attendance_session` เพราะความหมายและวงจรสถานะต่างจากการเช็คชื่อกิจกรรม
- เพิ่มเมนูจัดการรถในหน้ารายละเอียดค่ายของครู
- เพิ่มการ์ดรถในหน้ารายละเอียดค่ายของนักเรียน
- ใช้รูปแบบตรวจสิทธิ์ค่ายเป็นฐาน แต่เพิ่มข้อจำกัดระดับห้องและรถให้ละเอียดขึ้น

## 18. ข้อสรุปที่ยืนยันแล้ว

- ไม่มีปุ่มลงรถทั้งหมดแยกต่างหาก แต่มีตัวเลือก `ลงรถทุกคน` ตอนรถจอด
- รถมีเพียง `จอด` และ `กำลังเดินทาง`
- นักเรียนมีเพียง `ไม่ได้อยู่บนรถ` และ `อยู่บนรถ`
- ตอนรถจอด ครูเลือกได้ว่าจะล้างสถานะนักเรียนทั้งหมด หรือให้นักเรียนกดลงรถเอง
- นักเรียนต้องกดอยู่บนรถใหม่ทุกครั้งหลังจุดแวะ
- ครูจัดตำแหน่งครั้งแรกและบันทึกผัง จากนั้นนักเรียนห้ามสลับเอง
- ผังใช้เลขที่ระบบสร้าง ไม่อิงเลขที่นั่งจริงของรถทัวร์
- รองรับรถสองชั้นโดยแยกผังและเลขตำแหน่งตามชั้น
- นักเรียนในแต่ละห้องเรียนอยู่คนละคัน รถหนึ่งคันไม่มีนักเรียนจากหลายห้องปะปนกัน
