# แผนย้ายเมนูค่ายไปไว้ใน Sidebar

> ข้อสรุป: ย้ายเมนูการทำงานของค่ายจากการ์ดปุ่มในเนื้อหาหลักไปไว้ใน Sidebar ของหน้าค่าย โดยคงงานรถเป็นปุ่มแยกชื่อ `เช็คชื่อขึ้นรถ` และเปิดหน้าต่างของตัวเอง

## เป้าหมาย

- เมนูค่ายทั้งหมดอยู่ในตำแหน่งเดียวและจัดกลุ่มตามงาน
- ครูเข้าใช้งานเช็คชื่อปกติและเช็คชื่อขึ้นรถจากเมนูคนละรายการได้ชัดเจน
- หน้าแรกมีเฉพาะเมนูด่วน 3 รายการ: เช็คชื่อ, เช็คชื่อขึ้นรถ และติดตามนักเรียน
- เมนูด่วนทั้ง 3 รายการยังมีอยู่ใน Sidebar ด้วย เพื่อให้เข้าถึงจากเมนูหลักได้
- เมนูหลักทุกตัวเปิดเป็นหน้าเต็ม มี URL ของตัวเองและใช้ปุ่มย้อนกลับได้
- Modal เหลือเฉพาะงานย่อย เช่น สร้าง แก้ไข หรือลบข้อมูลภายในหน้า
- สิทธิ์ของครูหัวหน้าค่ายและครูทั่วไปยังถูกตรวจฝั่งเซิร์ฟเวอร์ตามค่ายและห้องเรียนที่รับผิดชอบ

## โครงสร้างหน้าจอหลังปรับ

ในหน้าค่ายให้ Sidebar แสดงเมนูเป็นกลุ่มดังนี้:

### ภาพรวม

- ภาพรวมค่าย

### ผู้เข้าร่วม

- ข้อมูลนักเรียน
- ติดตามนักเรียน
- เช็คชื่อนักเรียน

### การดำเนินงาน

- เช็คชื่อขึ้นรถ
  - เลือกรถหรือห้องเรียน
  - ดูจำนวน `อยู่บนรถ / นักเรียนที่จัดไว้`
  - ดูรายชื่อและผังที่นั่ง
  - จัดหรือแก้ไขตำแหน่งที่นั่งตามสิทธิ์เดิม
  - เปลี่ยนสถานะรถระหว่าง `จอด` และ `กำลังเดินทาง`
- รายการจองเสื้อ

### กิจกรรมและเอกสาร

- เอกสารโครงการ
- สร้างฐานกิจกรรม
- แบบสอบถาม
- เปรียบเทียบคะแนน

### ตั้งค่า

- ตั้งค่าเกียรติบัตร

การ์ดปุ่มชุดเดิมในเนื้อหาหลักถูกซ่อนออก เหลือเฉพาะเมนูด่วน 3 รายการ เพื่อไม่ให้เมนูอื่นซ้ำกับ Sidebar

## ชื่อที่ต้องเปลี่ยน

- เมนูเดิม `รถและผังที่นั่ง` → เปลี่ยนชื่อเป็น `เช็คชื่อขึ้นรถ`
- หัวข้อหน้าต่างเดิม `รถและผังที่นั่ง` → `เช็คชื่อขึ้นรถ`
- ข้อความเกี่ยวกับผังรถยังใช้คำว่า `ผังที่นั่ง` ได้ เพราะเป็นเครื่องมือภายในงานเช็คชื่อขึ้นรถ
- ชื่อ API และชื่อตารางฐานข้อมูลที่มีคำว่า `buses` ไม่ต้องเปลี่ยน เพื่อลดผลกระทบที่ไม่เกี่ยวกับหน้าจอ

## สิทธิ์การใช้งาน

### ครูหัวหน้าค่ายและแอดมิน

- เห็นเมนู Sidebar ครบทุกกลุ่ม
- เช็คชื่อนักเรียนได้ทุกห้องในค่าย
- สร้างและแก้ไขรถ จัดผังที่นั่ง และเปลี่ยนสถานะรถได้ทุกคันในค่าย

### ครูทั่วไป

- เห็นเฉพาะเมนูที่ได้รับสิทธิ์ใน Sidebar
- เช็คชื่อนักเรียนและดูข้อมูลรถเฉพาะห้องเรียนที่ตนรับผิดชอบ
- ใช้งานจัดผังและสถานะรถตามสิทธิ์เดิมของระบบ
- หากไม่มีห้องเรียนที่ได้รับมอบหมาย ให้แสดงข้อความว่าไม่มีรถหรือห้องเรียนที่รับผิดชอบ แทนการแสดงข้อมูลของห้องอื่น

## งานที่ต้องแก้ในโค้ด

### 1. เพิ่ม Sidebar สำหรับหน้าค่าย

ไฟล์ `app/headteacher/dashboard/layout.tsx`

- [x] แยกเมนูหน้าค่ายออกจาก Sidebar หลักของครู
- [x] เพิ่มกลุ่ม ภาพรวม, ผู้เข้าร่วม, การดำเนินงาน, กิจกรรมและเอกสาร, และตั้งค่า
- [x] ให้เมนู Sidebar ใช้ route ของหน้าเต็มแต่ละงาน
- [x] กรองเมนูเจ้าของค่ายและเมนูรถ/เสื้อตามสิทธิ์ค่าย
- [x] คงเมนูด่วนบนหน้าแรกไว้เฉพาะเช็คชื่อ, เช็คชื่อขึ้นรถ และติดตามนักเรียน
- [ ] ตรวจการแสดงผล Sidebar ทั้ง desktop และ mobile

### 2. ปรับชื่อการ์ดและหน้าต่างรถ

ไฟล์ `app/headteacher/dashboard/camp/CampDetailPage.tsx`

- [x] เปลี่ยนข้อความเมนู `รถและผังที่นั่ง` เป็น `เช็คชื่อขึ้นรถ`
- [x] เปลี่ยนหัวข้อ `BusManagementModal` เป็น `เช็คชื่อขึ้นรถ`
- [x] คง state `isBusModalOpen` และการเรียก `BusManagementModal` เป็นหน้าต่างแยก
- [x] ให้เมนูด่วนพาไปหน้า `เช็คชื่อนักเรียน` เต็มหน้า
- [ ] ตรวจให้ครูหัวหน้าค่ายและครูทั่วไปที่เกี่ยวข้องยังเห็นการ์ดเช็คชื่อ

### 3. คงหน้าต่างการทำงานแยกกัน

ไฟล์ `app/headteacher/dashboard/camp/AttendanceModal.tsx`

- [x] แยกหน้า `AttendanceModal` เป็น route เต็มหน้า โดยไม่มี Backdrop
- [x] เมนูด่วนและ Sidebar ชี้ไป route เดียวกัน

### 4. ตรวจหน้าต่างเช็คชื่อขึ้นรถ

ไฟล์ `app/headteacher/dashboard/camp/BusManagementModal.tsx`

- [x] เปลี่ยนหัวข้อที่ผู้ใช้เห็นเป็น `เช็คชื่อขึ้นรถ`
- [x] แยกหน้า `BusManagementModal` เป็น route `เช็คชื่อขึ้นรถ` เต็มหน้า
- [ ] คงความสามารถเลือกคันรถ สร้างรถ จัด/ล็อกผัง และเปลี่ยนสถานะรถไว้ครบ

### 5. คง API และสิทธิ์เดิม

ไฟล์ที่เกี่ยวข้อง:

- `app/api/camps/[id]/buses/route.ts`
- `app/api/camps/[id]/buses/[busId]/layout/route.ts`
- `app/api/camps/[id]/buses/[busId]/layout/lock/route.ts`
- `app/api/camps/[id]/buses/[busId]/layout/unlock/route.ts`
- `app/api/camps/[id]/buses/[busId]/status/route.ts`
- `lib/camp-bus-auth.ts`

งานตรวจสอบ:

- [ ] ไม่เปลี่ยน URL ของ API เพียงเพราะเปลี่ยนชื่อเมนู
- [ ] ยืนยันว่าหัวหน้าค่ายเห็นรถทุกคันในค่าย
- [ ] ยืนยันว่าครูทั่วไปเห็นเฉพาะห้องที่ตนรับผิดชอบ
- [ ] ทุก mutation ยังตรวจสิทธิ์ฝั่งเซิร์ฟเวอร์ ไม่พึ่งการซ่อนแท็บหรือปุ่มฝั่งหน้าเว็บ

## ลำดับการทำงาน

1. ตรวจ Sidebar และการกรองเมนูตามสิทธิ์
2. ตรวจการเปิดหน้า/Modal จากเมนูแต่ละรายการ
3. คง `AttendanceModal` และ `BusManagementModal` แยกกัน
4. ตรวจสิทธิ์ทั้งครูหัวหน้าค่ายและครูทั่วไป
5. ทดสอบ responsive บนมือถือ แท็บเล็ต และ desktop
6. รัน lint, type check และ build

## เกณฑ์ยอมรับงาน

- [x] Sidebar หน้าค่ายแสดงเมนูครบตามกลุ่มที่กำหนด
- [x] หน้าแรกแสดงเมนูด่วนเฉพาะ 3 รายการที่ใช้บ่อย
- [x] เมนูด่วนทั้ง 3 รายการมีอยู่ใน Sidebar ด้วย
- [x] หน้าเนื้อหาหลักไม่แสดงการ์ดปุ่มงานอื่นซ้ำกับ Sidebar
- [x] เมนูเปลี่ยนชื่อเป็น `เช็คชื่อขึ้นรถ`
- [x] กด `เช็คชื่อนักเรียน` แล้วเปิดเฉพาะหน้าต่างเช็คชื่อปกติ
- [x] กด `เช็คชื่อขึ้นรถ` แล้วเปิดหน้าต่างรถ ผังที่นั่ง และสถานะนักเรียน
- [ ] การจัดผัง ล็อก/ปลดล็อกผัง และเปลี่ยนสถานะรถยังทำงานได้
- [ ] ครูทั่วไปไม่เห็นข้อมูลรถของห้องอื่น
- [ ] การปิดหน้าต่างหนึ่งไม่ทำให้อีกหน้าต่างเปิดหรือเปลี่ยนสถานะโดยไม่ตั้งใจ
- [x] เมนูหลักไม่เปิด Modal ซ้อนหรือ Modal เป็นหน้าหลัก
- [ ] ใช้งานได้โดยไม่เกิด horizontal overflow บนหน้าจอมือถือ
- [ ] lint, type check และ build ผ่าน
